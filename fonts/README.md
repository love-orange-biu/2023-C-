# 微软雅黑字体放置目录

请把合法获得的微软雅黑常规字体文件复制到本目录，并命名为：

```text
msyh.ttc
```

最终路径应为：

```text
/home/orange/2023国赛C题/fonts/msyh.ttc
```

`第三题.py` 会通过 Matplotlib 的 `addfont` 直接注册该文件，因此在 Ubuntu 下不需要运行 `fc-cache`，也不需要修改系统字体目录。

微软雅黑是微软字体，项目不会自动下载或重新分发该字体。请确保字体来源和使用方式符合许可证。
